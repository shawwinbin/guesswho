#!/usr/bin/expect -f

set timeout 120
set password "35332280"
set host "192.168.31.19"
set user "shaw"

spawn scp /Users/shaw/history-figure-guess/deploy-setup.sh ${user}@${host}:/tmp/

expect {
    "password:" {
        send "${password}\r"
        exp_continue
    }
    eof {}
    timeout { exit 1 }
}

puts "脚本传输完成"